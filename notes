#Forum

1. Thread
2. Reply
3. User

A. Thread is created by a user
B. A reply belongs to a thread, and belongs to a user

episode 2 

3:24 
//use < php artisan ui vue --auth > to create Auth

5:08
//use < vendor/bin/phpunit > instead of [phpunit]

episode 3

4:17
// skip the test_a_user_can_read_replies_associated_with_a_thread() as it is a compatiblity issue with phpunit
